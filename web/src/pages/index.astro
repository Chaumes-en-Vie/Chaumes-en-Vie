---
const logoSrc = "/logo.png";
---

<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <title>Chaumes en Vie – Bientôt en ligne</title>
    <meta
      name="description"
      content="Site officiel de la liste citoyenne Chaumes en Vie à Chaumes-en-Brie. Bientôt en ligne."
    />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
  </head>
  <body>
    <main class="page">
      {/* blobs floutés en arrière-plan */}
      <div class="blob blob-yellow"></div>
      <div class="blob blob-blue"></div>
      <div class="blob blob-green"></div>

      {/* logo animé */}
      <div class="logo-wrapper">
        <img
          src={logoSrc}
          alt="Logo Chaumes en Vie"
          class="logo"
        />
      </div>

      {/* texte "à venir" */}
      <p class="soon">À venir bientôt…</p>
    </main>

    <style>
      :root {
        --cv-red: #c9184a;
        --cv-yellow: #f4a300;
        --cv-green: #007a3d;
        --cv-blue: #008fbf;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          sans-serif;
        background: #ffffff;
        color: #334155;
      }

      .page {
        position: relative;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        overflow: hidden;
      }

      .blob {
        position: absolute;
        border-radius: 999px;
        filter: blur(40px);
        opacity: 0.6;
        z-index: 0;
      }

      .blob-yellow {
        width: 280px;
        height: 280px;
        background: var(--cv-yellow);
        top: -80px;
        left: -80px;
      }

      .blob-blue {
        width: 320px;
        height: 320px;
        background: var(--cv-blue);
        bottom: -120px;
        right: -60px;
      }

      .blob-green {
        width: 220px;
        height: 220px;
        background: var(--cv-green);
        top: 40%;
        right: 10%;
      }

      .logo-wrapper {
        position: relative;
        z-index: 1;
        padding: 32px;
        border-radius: 999px;
        background: #ffffff;
        box-shadow: 0 18px 45px rgba(15, 23, 42, 0.25);
        animation: float 6s ease-in-out infinite;
      }

      .logo {
        display: block;
        width: min(260px, 70vw);
        height: auto;
      }

      .soon {
        position: relative;
        margin-top: 24px;
        z-index: 1;
        font-size: 0.95rem;
        color: #6b7280;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        font-weight: 500;
        animation: fadeIn 2.4s ease-out forwards;
      }

      @keyframes float {
        0% {
          transform: translateY(0px) scale(1);
        }
        50% {
          transform: translateY(-10px) scale(1.01);
        }
        100% {
          transform: translateY(0px) scale(1);
        }
      }

      @keyframes fadeIn {
        0% {
          opacity: 0;
          transform: translateY(8px);
          filter: blur(4px);
        }
        100% {
          opacity: 1;
          transform: translateY(0);
          filter: blur(0);
        }
      }
    </style>
  </body>
</html>
